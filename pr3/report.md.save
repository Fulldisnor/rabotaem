# ПР Nº3. Права доступа Linux и управление пользователями
## 1. Пользователи и группы
Созданные пользователи и их роли:
І Пользователь | Группа | Роль |
------|---||
alice | developers | Администратор проекта bob | developers | Разработчик ||
carol | auditors | Аудитор ||
Поля /etc/passwd (разбор строки alice) :
...
## 2. Права доступа chmod/chown
Итоговые права /srv/project/code: 'chmod 770' , владелец alice:devel
- bob (в группе developers): [мог/не мог] создать файл при 750 пото
-。…・
- carol (не в группе): [мог/не мог] войти в директорию потому что .
! [Скриншот 1s -la] (screens/1s_project.png)
## 3. ACL
Задача: дать carol доступ на чтение без изменения группы.
Команда: setfacl -m u:carol:r-x /srv/project/code'
Вывод getfacl после изменения:
= вставить вывод команды
## 4. sudo-политики
| Пользователь | Разрешено | Ограничение |
alice | Bcë (NOPASSWD) | He |
bob | apt, apt-get | Только пакетный менеджер | carol | journalctl, cat /var/log/* | Только просмотр логов |
## 5. PAM
Модуль pam unix.so в / etc/pam.d/sudo: ...
4rO O3Haga required：...
Как добавить минимальную длину пароля 12 символов: ...
## Выводы
